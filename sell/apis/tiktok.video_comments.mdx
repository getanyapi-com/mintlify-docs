---
title: "TikTok Video Comments"
description: "Sell tiktok.video_comments on AnyAPI: the request your endpoint receives, the response it must return, and what the live trial grades."
---

List the comments on a TikTok video by URL with cursor pagination (text, author, likes, reply count).

This page is the seller-facing summary of one catalog API, `tiktok.video_comments`. The transport rules that apply to every API a seller serves - signed requests, the response envelope, timeouts, and body limits - are on [the operator endpoint contract](/sell/operator-endpoint-contract).

## The price you compete with

AnyAPI publishes this API from **$0.70 per 1,000 requests**. That is the price a buyer sees today, so it is the price your endpoint has to come in under to win traffic.

## The request your endpoint receives

| Field | Type | Required | What it means |
| --- | --- | --- | --- |
| `cursor` | string | no | Pagination cursor from a previous response's nextCursor. |
| `url` | string | yes | Full TikTok video URL. |

<Accordion title="Full input JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/tiktok.video_comments.input.json",
  "example": {
    "url": "https://www.tiktok.com/@zachking/video/7650468599424945422?_r=1&u_code=f0hj7d780760m9&preview_pb=0&sharer_language=en&_d=f0hj7blh067h71&share_item_id=7650468599424945422&source=h5_m"
  },
  "title": "TikTok video comments input",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "url"
  ],
  "properties": {
    "url": {
      "type": "string",
      "description": "Full TikTok video URL."
    },
    "cursor": {
      "type": "string",
      "description": "Pagination cursor from a previous response's nextCursor."
    }
  }
}
```

</Accordion>

## The response your endpoint must return

| Field | Type | Always present | What it means |
| --- | --- | --- | --- |
| `data` | null or object | yes |  |
| `data.comments` | array | yes |  |
| `data.comments[].author` | string | yes |  |
| `data.comments[].authorImage` | string | no | Comment author's avatar image URL. Signed and short-lived, so the query string is load-bearing and kept intact. |
| `data.comments[].authorName` | string | no | Comment author's display name. |
| `data.comments[].createdUtc` | number | yes | UTC epoch timestamp in seconds (Unix time). Multiply by 1000 for a JS Date in milliseconds. |
| `data.comments[].id` | string | yes |  |
| `data.comments[].likes` | integer | yes |  |
| `data.comments[].replies` | integer | yes |  |
| `data.comments[].text` | string | yes |  |
| `data.comments[].videoId` | string | no | Identifier of the video the comment belongs to. |
| `data.nextCursor` | string or null | yes | Opaque cursor for the next page of comments, or null when this lane has no more. Pass it back as cursor to continue. |
| `found` | boolean | yes |  |

Extra fields are kept, missing declared fields fail.

<Accordion title="Full output JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/tiktok.video_comments.output.json",
  "title": "TikTok video comments output",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "found",
    "data"
  ],
  "properties": {
    "found": {
      "type": "boolean"
    },
    "data": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "comments",
            "nextCursor"
          ],
          "properties": {
            "nextCursor": {
              "type": [
                "string",
                "null"
              ],
              "description": "Opaque cursor for the next page of comments, or null when this lane has no more. Pass it back as cursor to continue."
            },
            "comments": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "id",
                  "text",
                  "author",
                  "likes",
                  "replies",
                  "createdUtc"
                ],
                "properties": {
                  "id": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "text": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "author": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "likes": {
                    "type": "integer"
                  },
                  "replies": {
                    "type": "integer"
                  },
                  "createdUtc": {
                    "type": "number",
                    "description": "UTC epoch timestamp in seconds (Unix time). Multiply by 1000 for a JS Date in milliseconds."
                  },
                  "videoId": {
                    "type": "string",
                    "description": "Identifier of the video the comment belongs to."
                  },
                  "authorName": {
                    "type": "string",
                    "description": "Comment author's display name."
                  },
                  "authorImage": {
                    "type": "string",
                    "description": "Comment author's avatar image URL. Signed and short-lived, so the query string is load-bearing and kept intact."
                  }
                }
              }
            }
          }
        }
      ]
    }
  }
}
```

</Accordion>

## Fields that must never come back empty

- `author`
- `id`
- `text`

The repository marks these fields as ones every source must be able to fill. An empty value is only correct when the entity itself genuinely has nothing there.

## Not found, and how it differs from empty

When the entity does not exist, answer 2xx with the not-found shape:

```json
{
  "found": false,
  "data": null
}
```

An empty list is not a not-found: when the entity exists and has nothing to return, answer 2xx with `found: true` and an empty `comments` array.

This output schema declares no `reason` field, so `found: false` is the whole not-found answer.

## Example request

```json
{
  "url": "https://www.tiktok.com/@zachking/video/7650468599424945422?_r=1&u_code=f0hj7d780760m9&preview_pb=0&sharer_language=en&_d=f0hj7blh067h71&share_item_id=7650468599424945422&source=h5_m"
}
```

The compatibility checks fire the schema's example at your endpoint. After that, your answers are graded against what the currently serving source returned for the same real customer inputs.

## Next

- [The operator endpoint contract](/sell/operator-endpoint-contract) for signing, budgets, and the live trial.
- [Every API open to sellers](/sell/apis/index).
- Want to sell an API that is not on that list? Email [support@getanyapi.com](mailto:support@getanyapi.com).
