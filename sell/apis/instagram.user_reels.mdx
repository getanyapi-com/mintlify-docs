---
title: "Instagram User Reels"
description: "Sell instagram.user_reels on AnyAPI: the request your endpoint receives, the response it must return, and what the live trial grades."
---

List an Instagram account's reels by handle with cursor pagination (caption, plays, likes, comments).

This page is the seller-facing summary of one catalog API, `instagram.user_reels`. The transport rules that apply to every API a seller serves - signed requests, the response envelope, timeouts, and body limits - are on [the operator endpoint contract](/sell/operator-endpoint-contract).

## The price you compete with

AnyAPI publishes this API from **$0.80 per 1,000 requests**. That is the price a buyer sees today, so it is the price your endpoint has to come in under to win traffic.

## The request your endpoint receives

This API takes no input fields.

<Accordion title="Full input JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/instagram.user_reels.input.json",
  "example": {
    "handle": "nasa"
  },
  "title": "Instagram user reels input",
  "type": "object",
  "additionalProperties": false,
  "x-aliases": {
    "username": "handle"
  },
  "properties": {
    "handle": {
      "type": "string",
      "description": "Instagram handle."
    },
    "userId": {
      "type": "string",
      "description": "Instagram user id (faster than handle when known)."
    },
    "cursor": {
      "type": "string",
      "description": "Pagination cursor (max_id) from a previous response's nextCursor."
    }
  },
  "anyOf": [
    {
      "required": [
        "handle"
      ]
    },
    {
      "required": [
        "userId"
      ]
    }
  ]
}
```

</Accordion>

## The response your endpoint must return

| Field | Type | Always present | What it means |
| --- | --- | --- | --- |
| `data` | null or object | yes |  |
| `data.nextCursor` | string or null | yes | Opaque cursor for the next page of reels, or null when this lane has no more. Pass it back as cursor to continue. |
| `data.reels` | array | yes |  |
| `data.reels[].avatarUrl` | string | no | Profile picture URL of the posting account. |
| `data.reels[].caption` | string | yes |  |
| `data.reels[].comments` | integer | yes |  |
| `data.reels[].createdUtc` | number | yes | UTC epoch timestamp in seconds (Unix time). Multiply by 1000 for a JS Date in milliseconds. |
| `data.reels[].displayName` | string | no | Display name of the posting account. |
| `data.reels[].durationSeconds` | number | yes | Reel duration in seconds. |
| `data.reels[].id` | string | yes | Instagram media id. |
| `data.reels[].likes` | integer | yes |  |
| `data.reels[].media` | array | no | Photo, video, and GIF attachments on the post. Empty when the post has none. |
| `data.reels[].media[].height` | integer | no | Pixel height of the media item, when the lane reports it. |
| `data.reels[].media[].type` | string | yes | One of photo, video, or gif. |
| `data.reels[].media[].url` | string | yes | Image URL. For a video or GIF this is the poster/thumbnail frame. |
| `data.reels[].media[].videoUrl` | string | no | Playable video file URL. Present only for video and gif items. |
| `data.reels[].media[].width` | integer | no | Pixel width of the media item, when the lane reports it. |
| `data.reels[].shortcode` | string | yes |  |
| `data.reels[].userId` | string | no | Instagram user id of the posting account. |
| `data.reels[].username` | string | yes | Username of the account that posted the reel. A reels tab includes collaborator reels, so this is not always the requested handle. |
| `data.reels[].verified` | boolean | yes | True when the posting account is verified. |
| `data.reels[].views` | integer | yes |  |
| `found` | boolean | yes |  |

Extra fields are kept, missing declared fields fail.

<Accordion title="Full output JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/instagram.user_reels.output.json",
  "title": "Instagram user reels output",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "found",
    "data"
  ],
  "properties": {
    "found": {
      "type": "boolean"
    },
    "data": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "reels",
            "nextCursor"
          ],
          "properties": {
            "nextCursor": {
              "type": [
                "string",
                "null"
              ],
              "description": "Opaque cursor for the next page of reels, or null when this lane has no more. Pass it back as cursor to continue."
            },
            "reels": {
              "x-anyapi-must-populate": true,
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "id",
                  "shortcode",
                  "caption",
                  "views",
                  "likes",
                  "comments",
                  "createdUtc",
                  "username",
                  "verified",
                  "durationSeconds"
                ],
                "properties": {
                  "id": {
                    "x-anyapi-must-populate": true,
                    "type": "string",
                    "description": "Instagram media id."
                  },
                  "shortcode": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "caption": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "views": {
                    "type": "integer"
                  },
                  "likes": {
                    "type": "integer"
                  },
                  "comments": {
                    "type": "integer"
                  },
                  "createdUtc": {
                    "type": "number",
                    "description": "UTC epoch timestamp in seconds (Unix time). Multiply by 1000 for a JS Date in milliseconds."
                  },
                  "username": {
                    "x-anyapi-must-populate": true,
                    "type": "string",
                    "description": "Username of the account that posted the reel. A reels tab includes collaborator reels, so this is not always the requested handle."
                  },
                  "verified": {
                    "type": "boolean",
                    "description": "True when the posting account is verified."
                  },
                  "durationSeconds": {
                    "type": "number",
                    "description": "Reel duration in seconds."
                  },
                  "displayName": {
                    "type": "string",
                    "description": "Display name of the posting account."
                  },
                  "avatarUrl": {
                    "type": "string",
                    "description": "Profile picture URL of the posting account."
                  },
                  "userId": {
                    "type": "string",
                    "description": "Instagram user id of the posting account."
                  },
                  "media": {
                    "type": "array",
                    "description": "Photo, video, and GIF attachments on the post. Empty when the post has none.",
                    "items": {
                      "type": "object",
                      "required": [
                        "type",
                        "url"
                      ],
                      "properties": {
                        "type": {
                          "type": "string",
                          "description": "One of photo, video, or gif."
                        },
                        "url": {
                          "type": "string",
                          "format": "uri",
                          "description": "Image URL. For a video or GIF this is the poster/thumbnail frame."
                        },
                        "videoUrl": {
                          "type": "string",
                          "format": "uri",
                          "description": "Playable video file URL. Present only for video and gif items."
                        },
                        "width": {
                          "type": "integer",
                          "description": "Pixel width of the media item, when the lane reports it."
                        },
                        "height": {
                          "type": "integer",
                          "description": "Pixel height of the media item, when the lane reports it."
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      ]
    }
  }
}
```

</Accordion>

## Fields that must never come back empty

- `caption`
- `id`
- `reels`
- `shortcode`
- `username`

The repository marks these fields as ones every source must be able to fill. An empty value is only correct when the entity itself genuinely has nothing there.

## Not found, and how it differs from empty

When the entity does not exist, answer 2xx with the not-found shape:

```json
{
  "found": false,
  "data": null
}
```

An empty list is not a not-found: when the entity exists and has nothing to return, answer 2xx with `found: true` and an empty `reels` array.

This output schema declares no `reason` field, so `found: false` is the whole not-found answer.

## Example request

```json
{
  "handle": "nasa"
}
```

The compatibility checks fire the schema's example at your endpoint. After that, your answers are graded against what the currently serving source returned for the same real customer inputs.

## Next

- [The operator endpoint contract](/sell/operator-endpoint-contract) for signing, budgets, and the live trial.
- [Every API open to sellers](/sell/apis/index).
- Want to sell an API that is not on that list? Email [support@getanyapi.com](mailto:support@getanyapi.com).
