---
title: "Instagram Basic Profile"
description: "Sell instagram.basic_profile on AnyAPI: the request your endpoint receives, the response it must return, and what the live trial grades."
---

Fetch an Instagram account's core public profile fields (followers, posts, bio, verification) by user id.

This page is the seller-facing summary of one catalog API, `instagram.basic_profile`. The transport rules that apply to every API a seller serves - signed requests, the response envelope, timeouts, and body limits - are on [the operator endpoint contract](/sell/operator-endpoint-contract).

## The price you compete with

AnyAPI publishes this API from **$1.20 per 1,000 requests**. That is the price a buyer sees today, so it is the price your endpoint has to come in under to win traffic.

## The request your endpoint receives

| Field | Type | Required | What it means |
| --- | --- | --- | --- |
| `userId` | string | yes | Instagram numeric user id (the account's internal all-digits id, e.g. 314216), NOT the @handle or profile URL. Passing a handle here returns no profile. |

<Accordion title="Full input JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/instagram.basic_profile.input.json",
  "example": {
    "userId": "314216"
  },
  "title": "Instagram Basic Profile input",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "userId"
  ],
  "properties": {
    "userId": {
      "type": "string",
      "description": "Instagram numeric user id (the account's internal all-digits id, e.g. 314216), NOT the @handle or profile URL. Passing a handle here returns no profile."
    }
  }
}
```

</Accordion>

## The response your endpoint must return

| Field | Type | Always present | What it means |
| --- | --- | --- | --- |
| `data` | null or object | yes |  |
| `data.avatarUrl` | string | yes |  |
| `data.bio` | string | yes |  |
| `data.displayName` | string | yes |  |
| `data.externalUrl` | string | yes |  |
| `data.followers` | integer | yes |  |
| `data.following` | integer | yes |  |
| `data.handle` | string | yes |  |
| `data.private` | boolean | yes |  |
| `data.userId` | string | yes |  |
| `data.verified` | boolean | yes |  |
| `found` | boolean | yes |  |

Extra fields are kept, missing declared fields fail.

<Accordion title="Full output JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/instagram.basic_profile.output.json",
  "title": "Instagram Basic Profile output",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "found",
    "data"
  ],
  "properties": {
    "found": {
      "type": "boolean"
    },
    "data": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "required": [
            "userId",
            "handle",
            "displayName",
            "followers",
            "following",
            "bio",
            "verified",
            "private",
            "externalUrl",
            "avatarUrl"
          ],
          "properties": {
            "userId": {
              "x-anyapi-must-populate": true,
              "type": "string"
            },
            "handle": {
              "x-anyapi-must-populate": true,
              "type": "string"
            },
            "displayName": {
              "x-anyapi-must-populate": true,
              "type": "string"
            },
            "followers": {
              "type": "integer"
            },
            "following": {
              "type": "integer"
            },
            "bio": {
              "type": "string"
            },
            "verified": {
              "type": "boolean"
            },
            "private": {
              "type": "boolean"
            },
            "externalUrl": {
              "type": "string"
            },
            "avatarUrl": {
              "x-anyapi-must-populate": true,
              "type": "string"
            }
          }
        }
      ]
    }
  }
}
```

</Accordion>

## Fields that must never come back empty

- `avatarUrl`
- `displayName`
- `handle`
- `userId`

The repository marks these fields as ones every source must be able to fill. An empty value is only correct when the entity itself genuinely has nothing there.

## Not found, and how it differs from empty

When the entity does not exist, answer 2xx with the not-found shape:

```json
{
  "found": false,
  "data": null
}
```

An empty result is not a not-found: when the entity exists but carries nothing, answer 2xx with `found: true` and the empty values.

This output schema declares no `reason` field, so `found: false` is the whole not-found answer.

## Example request

```json
{
  "userId": "314216"
}
```

The compatibility checks fire the schema's example at your endpoint. After that, your answers are graded against what the currently serving source returned for the same real customer inputs.

## Next

- [The operator endpoint contract](/sell/operator-endpoint-contract) for signing, budgets, and the live trial.
- [Every API open to sellers](/sell/apis/index).
- Want to sell an API that is not on that list? Email [support@getanyapi.com](mailto:support@getanyapi.com).
