---
title: "TikTok Profile Videos"
description: "Sell tiktok.profile_videos on AnyAPI: the request your endpoint receives, the response it must return, and what the live trial grades."
---

List a TikTok creator's recent videos (views, likes, comments) by handle with cursor pagination.

This page is the seller-facing summary of one catalog API, `tiktok.profile_videos`. The transport rules that apply to every API a seller serves - signed requests, the response envelope, timeouts, and body limits - are on [the operator endpoint contract](/sell/operator-endpoint-contract).

## The price you compete with

AnyAPI publishes this API from **$0.70 per 1,000 requests**. That is the price a buyer sees today, so it is the price your endpoint has to come in under to win traffic.

## The request your endpoint receives

| Field | Type | Required | What it means |
| --- | --- | --- | --- |
| `cursor` | string | no | Pagination cursor from a previous response's nextCursor. |
| `handle` | string | yes | TikTok username without the leading @. |

<Accordion title="Full input JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/tiktok.profile_videos.input.json",
  "example": {
    "handle": "zachking"
  },
  "title": "TikTok profile videos input",
  "type": "object",
  "additionalProperties": false,
  "x-aliases": {
    "username": "handle"
  },
  "required": [
    "handle"
  ],
  "properties": {
    "handle": {
      "type": "string",
      "description": "TikTok username without the leading @."
    },
    "cursor": {
      "type": "string",
      "description": "Pagination cursor from a previous response's nextCursor."
    }
  }
}
```

</Accordion>

## The response your endpoint must return

| Field | Type | Always present | What it means |
| --- | --- | --- | --- |
| `data` | null or object | yes |  |
| `data.nextCursor` | string or null | yes | Opaque cursor for the next page of videos, or null when this lane has no more. Pass it back as cursor to continue. |
| `data.videos` | array | yes |  |
| `data.videos[].authorHandle` | string | no | Author's TikTok handle, without the leading @. |
| `data.videos[].authorImage` | string | no | Author's avatar image URL. Signed and short-lived, so the query string is load-bearing and kept intact. |
| `data.videos[].authorName` | string | no | Author's display name. |
| `data.videos[].authorUserId` | string | no | Author's numeric TikTok user id. |
| `data.videos[].caption` | string | yes |  |
| `data.videos[].comments` | integer | yes |  |
| `data.videos[].createdUtc` | number | yes | UTC epoch timestamp in seconds (Unix time). Multiply by 1000 for a JS Date in milliseconds. |
| `data.videos[].durationSeconds` | number | no | Video duration in seconds. |
| `data.videos[].id` | string | yes |  |
| `data.videos[].image` | string | no | URL of the video's cover/thumbnail image. A signed, short-lived TikTok CDN URL (typically expires within about a day; query params are load-bearing and kept intact), often served as HEIC rather than JPEG, so fetch it promptly and transcode if you need broad browser support. Absent when the upstream provides no cover. |
| `data.videos[].likes` | integer | yes |  |
| `data.videos[].saves` | integer | no |  |
| `data.videos[].shares` | integer | no | Share count. |
| `data.videos[].url` | string | yes |  |
| `data.videos[].videoUrl` | string | no | Playable video URL. Signed and short-lived, so the query string is load-bearing and kept intact. |
| `data.videos[].views` | integer | yes |  |
| `found` | boolean | yes |  |

Extra fields are kept, missing declared fields fail.

<Accordion title="Full output JSON Schema">

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getanyapi.com/schemas/tiktok.profile_videos.output.json",
  "title": "TikTok profile videos output",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "found",
    "data"
  ],
  "properties": {
    "found": {
      "type": "boolean"
    },
    "data": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "videos",
            "nextCursor"
          ],
          "properties": {
            "nextCursor": {
              "type": [
                "string",
                "null"
              ],
              "description": "Opaque cursor for the next page of videos, or null when this lane has no more. Pass it back as cursor to continue."
            },
            "videos": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "id",
                  "url",
                  "caption",
                  "views",
                  "likes",
                  "comments",
                  "createdUtc"
                ],
                "properties": {
                  "id": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "url": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "caption": {
                    "x-anyapi-must-populate": true,
                    "type": "string"
                  },
                  "image": {
                    "type": "string",
                    "description": "URL of the video's cover/thumbnail image. A signed, short-lived TikTok CDN URL (typically expires within about a day; query params are load-bearing and kept intact), often served as HEIC rather than JPEG, so fetch it promptly and transcode if you need broad browser support. Absent when the upstream provides no cover."
                  },
                  "views": {
                    "type": "integer"
                  },
                  "likes": {
                    "type": "integer"
                  },
                  "comments": {
                    "type": "integer"
                  },
                  "createdUtc": {
                    "x-anyapi-must-populate": true,
                    "type": "number",
                    "description": "UTC epoch timestamp in seconds (Unix time). Multiply by 1000 for a JS Date in milliseconds."
                  },
                  "saves": {
                    "type": "integer"
                  },
                  "shares": {
                    "type": "integer",
                    "description": "Share count."
                  },
                  "durationSeconds": {
                    "type": "number",
                    "description": "Video duration in seconds."
                  },
                  "videoUrl": {
                    "type": "string",
                    "description": "Playable video URL. Signed and short-lived, so the query string is load-bearing and kept intact."
                  },
                  "authorHandle": {
                    "type": "string",
                    "description": "Author's TikTok handle, without the leading @."
                  },
                  "authorName": {
                    "type": "string",
                    "description": "Author's display name."
                  },
                  "authorUserId": {
                    "type": "string",
                    "description": "Author's numeric TikTok user id."
                  },
                  "authorImage": {
                    "type": "string",
                    "description": "Author's avatar image URL. Signed and short-lived, so the query string is load-bearing and kept intact."
                  }
                }
              }
            }
          }
        }
      ]
    }
  }
}
```

</Accordion>

## Fields that must never come back empty

- `caption`
- `createdUtc`
- `id`
- `url`

The repository marks these fields as ones every source must be able to fill. An empty value is only correct when the entity itself genuinely has nothing there.

## Not found, and how it differs from empty

When the entity does not exist, answer 2xx with the not-found shape:

```json
{
  "found": false,
  "data": null
}
```

An empty list is not a not-found: when the entity exists and has nothing to return, answer 2xx with `found: true` and an empty `videos` array.

This output schema declares no `reason` field, so `found: false` is the whole not-found answer.

## Example request

```json
{
  "handle": "zachking"
}
```

The compatibility checks fire the schema's example at your endpoint. After that, your answers are graded against what the currently serving source returned for the same real customer inputs.

## Next

- [The operator endpoint contract](/sell/operator-endpoint-contract) for signing, budgets, and the live trial.
- [Every API open to sellers](/sell/apis/index).
- Want to sell an API that is not on that list? Email [support@getanyapi.com](mailto:support@getanyapi.com).
