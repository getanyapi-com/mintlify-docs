---
title: "n8n community node"
description: "Run any AnyAPI endpoint inside an n8n workflow on one credential, billed per request in USD."
---

The official [n8n](https://n8n.io) community node runs any API in the catalog inside a
workflow. Add one credential, add the **AnyAPI** node, pick an API from the dropdown, and
pass its input.

| | |
|---|---|
| **Package** | [`n8n-nodes-anyapi`](https://www.npmjs.com/package/n8n-nodes-anyapi) |
| **Source** | [github.com/getanyapi-com/n8n-nodes-anyapi](https://github.com/getanyapi-com/n8n-nodes-anyapi) |
| **Node** | **AnyAPI** (operations include Run API, Get/Wait Request, discovery, and balance) |
| **Credential** | **AnyAPI API** (paste your key; Bearer auth) |

## Prerequisites

- An AnyAPI key. Create one in the [dashboard](https://getanyapi.com/dashboard) under **API keys**.
- A funded USD wallet. **Run API** bills your wallet per request; discovery is free. New
  accounts start with a small free balance.
- An n8n instance where you can install community nodes (self-hosted, or any plan that allows them).

## Install

<Steps>
  <Step title="Open community nodes">
    In n8n, go to **Settings > Community Nodes** and select **Install**.
  </Step>
  <Step title="Install the package">
    Enter the package name and confirm:

    ```
    n8n-nodes-anyapi
    ```

    See n8n's
    [community nodes installation guide](https://docs.n8n.io/integrations/community-nodes/installation/).
  </Step>
  <Step title="Add the AnyAPI node">
    In any workflow, search for **AnyAPI** in the node panel and add it.
  </Step>
</Steps>

## Create the AnyAPI credential

<Steps>
  <Step title="Add a new credential">
    On the AnyAPI node, create a new **AnyAPI API** credential.
  </Step>
  <Step title="Paste your key">
    Paste your AnyAPI key into **API Key**. It is sent as `Authorization: Bearer <key>`.

    Leave **Base URL** at its default, `https://api.getanyapi.com`. Change it only for a
    self-hosted or staging gateway.
  </Step>
  <Step title="Save">
    On save, n8n checks the key against the wallet balance endpoint and rejects an invalid key.
  </Step>
</Steps>

## Operations

<AccordionGroup>
  <Accordion title="Run API - execute any API by SKU" icon="bolt">
    Run an API by its SKU with a JSON **Input** payload. The **API** dropdown loads live from
    the public catalog.

    Returns the normalized `output`, `provider` (always **AnyAPI**), `costUsd`, and `items`
    (the number of results charged for). A failed call is free; see
    [Not found vs error](/sdks#not-found-vs-error) for when a miss is billed.

    The node polls long-running calls until they finish. Enable **Return Immediately** to emit a
    resumable Request ID. The node sends the paid POST once.
  </Accordion>
  <Accordion title="Get or Wait Request - resume long-running work" icon="clock">
    **Get Request** returns the current stored state. **Wait for Request** polls that state until the
    terminal result is ready. Connect the Request ID from a **Run API** node configured with
    **Return Immediately**.
  </Accordion>
  <Accordion title="Get API Schema - inspect input and output" icon="file-lines">
    Fetch one API's input and output JSON Schema. Build the **Run API** payload from the input schema.
  </Accordion>
  <Accordion title="List APIs - browse the catalog" icon="list">
    Browse the AnyAPI catalog, optionally narrowed with **Category** (a category slug).
    Returns one item per API.
  </Accordion>
  <Accordion title="Search APIs - find an API by intent" icon="magnifying-glass">
    Run a ranked free-text catalog search, optionally narrowed with **Category** or
    **Platform**. Results include each API's description and relevance.
  </Accordion>
  <Accordion title="Get Balance - check your wallet" icon="wallet">
    Return the remaining wallet balance in USD for the key on the credential. Never charges.
  </Accordion>
</AccordionGroup>

## Pricing fields

Discovery operations show prices as the gateway publishes them: `pricing.from` is the
primary static offer, `pricing.failoverMaxUsd` is the fallback ceiling, and `failover`
reports whether AnyAPI currently has an alternate route. The node never calculates these
from `lanes`. See [Pricing](/#pricing) for every price field.

## Response budget options (Run API)

**Run API** has three optional controls under **Options** that shrink the response. They never
change what you are billed (`costUsd` reflects the full result the API produced):

| Option | Effect |
|---|---|
| **Fields** | Comma-separated keys to keep on each result item. Dotted paths like `author.name` are allowed. |
| **Max Items** | Cap the number of result items returned (`0` means no cap). |
| **Summary Only** | Return only a structural outline instead of the full data. |

## Worked example

Add an **AnyAPI** node, select **Run API**, pick **Reddit Search** (`reddit.search`) from the
**API** dropdown, and set **Input** to:

```json
{ "query": "apify alternative" }
```

Execute. The node returns:

```json
{
  "output": {
    "posts": [
      { "title": "Looking for an Apify alternative", "url": "https://reddit.com/...", "score": 128 }
    ]
  },
  "provider": "AnyAPI",
  "costUsd": 0.003,
  "items": 25
}
```

Every API returns the same envelope on the same credential, so one workflow can run several
AnyAPI nodes and combine them with a **Merge** node.

<Tip>
  The same catalog is available over REST (see the [Quickstart](/quickstart)) and the
  [MCP server](/mcp-server). Need help? Email
  [support@getanyapi.com](mailto:support@getanyapi.com).
</Tip>
